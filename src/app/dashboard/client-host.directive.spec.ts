import { DashboardDirective } from './dashboard.directive';

describe('ClientHostDirective', () => {
  it('should create an instance', () => {
    const directive = new DashboardDirective();
    expect(directive).toBeTruthy();
  });
});
