import { ClientHostDirective } from './client-host.directive';

describe('ClientHostDirective', () => {
  it('should create an instance', () => {
    const directive = new ClientHostDirective();
    expect(directive).toBeTruthy();
  });
});
